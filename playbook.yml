---
- name: SETUP BIG-IP
  hosts: localhost
  vars_prompt:
    - name: username
      prompt: Please provide the username
    - name: password
      prompt: password
    
  tasks:
      - name: Create UCS File
        bigip_ucs_fetch:
          src: BKP_BIG-IP.ucs
          only_create_file: true
          provider:
            server: 192.168.117.2
            user: '{{ username }}'
            password: '{{ password }}'
            validate_certs: no
        delegate_to: localhost

      - name: Download UCS File
        bigip_ucs_fetch:
          src: teste_backup.ucs
          dest: /Backups/BKP_BIG-IP.ucs
          provider:
            server: 192.168.117.2
            user: '{{ username }}'
            password: '{{ password }}'
            validate_certs: no
        delegate_to: localhost
